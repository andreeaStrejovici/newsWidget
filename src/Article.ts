export class Article {
    title: string;
    source: string;
    date: string
    constructor(title : string,
        source : string,
        date : string) {
            this.title = title
            this.source = source
            this.date = date
        }
}